import 'package:flutter/material.dart';

class MemberStatsRow extends StatelessWidget {
  const MemberStatsRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}