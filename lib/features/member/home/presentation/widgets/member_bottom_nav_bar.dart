import 'package:flutter/material.dart';

class MemberBottomNavBar extends StatelessWidget {
  const MemberBottomNavBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}